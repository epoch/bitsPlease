

function greeting(greeting = 'hello', firstname = 'd', lastname = 't') {
  // return greeting + ' ' + firstname + ', how are you?' 
  return `${greeting} ${firstname}, how are you?`
}

// 'hello d, how are you?'


