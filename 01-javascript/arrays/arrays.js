var pm = ['turnbull', 'rudd', 'howard', 'hawk', 'keatin'];
var suffix = ['st', 'nd', 'rd', 'th', 'th']

for (var index = 0; index < pm.length; index++) {
  console.log('my number '+ (index+1) + suffix[index] +' choice is ' + pm[index]);
}

var languages = ['python', 'c++', 'perl']

